// Empty stub for the `server-only` package under vitest.
// The real package throws when imported outside a React Server Component build;
// vitest.config.ts aliases `server-only` here so server modules can be unit-tested.
export {};
